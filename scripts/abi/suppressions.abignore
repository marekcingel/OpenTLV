# libabigail suppression rules for scripts/abi/check.py.
#
# Add a rule only for a confirmed implementation detail that is not part of the
# public ABI, and say why. Private symbols are already hidden by
# -fvisibility=hidden, so this file is normally empty. Example:
#
# [suppress_type]
#   name = tlv_internal_state
